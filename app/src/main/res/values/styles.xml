<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="primary">@color/colorPrimary</item>
        <item name="dark">@color/colorPrimaryDark</item>
        <item name="accent">@color/colorAccent</item>
        <item name="background">@color/colorBackgroundLight</item>
        <item name="main">@color/accentMain</item>
        <item name="text">@color/textDark</item>
    </style>

</resources>
